#!/usr/bin/env python3
"""Wrapper for offline location normalization. The CLI is the real entry point (Milestone D)."""

from __future__ import annotations


def main() -> None:
    print(
        "Location normalization runs through the CLI once Milestone D lands:\n"
        "  uv run codetalent locations normalize"
    )
    raise SystemExit(2)


if __name__ == "__main__":
    main()
